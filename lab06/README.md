# Lab 06 — MCP Expansion for Your SDLC

**Duration:** ~75 min  
**SDLC Phase:** Plan → Implement → Validate → Release  
**Autonomy Level:** 🟡 Agent executes, human approves each story transition  
**Prerequisites:** Python 3.10+, VS Code with GitHub Copilot (Agent Mode enabled)

---

## Learning Objective

Use **MCP-connected project trackers** as the collaboration backbone of a real development sprint. You will manage issues, post implementation plans, transition story states, attach evidence, and write a release sign-off — all through Copilot prompts — while implementing the code locally.

This lab has **two parallel tracks** that follow the exact same steps but use different SDLC tools:

| | Track A | Track B |
|--|---------|---------|
| **Tracker** | Jira Cloud | Azure DevOps Boards |
| **MCP server** | `jira` | `azure-devops` |
| **Code editing** | Local (VS Code) | Local (VS Code) |
| **Key operations** | `execute_jql`, `transition_ticket`, `add_comment` | WIQL queries, `update_work_item`, `add_work_item_comment` |

Both tracks implement the same feature, produce the same evidence artifact, and end with the same release sign-off. Only the tracker differs.

> Choose your track and follow the **Track A** or **Track B** prompt block at each step.

---

## Use Case

Your team is delivering a **Manager Approval Reminder** capability for an HR workflow system (`FR-001`). Approvals pending more than 48 hours must trigger a reminder with a full audit trail.

Your tracker already contains the epic and five stories that map directly to five functions you will implement in `reminder_engine.py`.

---

## What You'll Practice

| Step | Activity | Track A (Jira) | Track B (Azure DevOps) |
|------|----------|----------------|------------------------|
| 1 | Verify connectivity & list backlog | `execute_jql` | `search_work_items` |
| 2 | Start first story | `transition_ticket`, `assign_ticket` | `update_work_item` |
| 3 | Post start comment | `add_comment` | `add_work_item_comment` |
| 4 | Create implementation plan | `add_comment` | `add_work_item_comment` |
| 5–8 | Implement, test, update tracker | `transition_ticket`, `add_comment` | `update_work_item`, `add_work_item_comment` |
| 9 | Release sign-off | `add_comment`, `transition_ticket` | `add_work_item_comment`, `update_work_item` |

---

## Code Structure

```
lab06/
  reminder_engine.py          ← implement your TODOs here
  run_sprint_demo.py          ← generates evidence/fr001_scheduler_run.json
  requirements.txt
  tests/
    test_reminder_engine.py   ← pre-written tests (fail until implemented)
  solutions/
    reminder_engine_solution.py       ← reference (peek only if stuck)
    test_reminder_engine_solution.py  ← reference tests (peek only if stuck)
  evidence/
    fr001_scheduler_run.json  ← generated by run_sprint_demo.py
```

Each TODO in `reminder_engine.py` maps directly to a story:

| Story | Jira key | ADO title | Function(s) |
|-------|----------|-----------|------------|
| Policy | SCRUM-2 | Define reminder policy | `validate_policy` |
| Data model | SCRUM-5 | Design reminder data model | `Approval`, `ReminderAttempt`, `SchedulerRunResult` |
| Eligibility | SCRUM-6 | Implement eligibility detection | `is_eligible_for_reminder` |
| Scheduler | SCRUM-3 | Build reminder scheduler | `send_reminder`, `run_scheduler_cycle` |
| Evidence | SCRUM-4 | Build evidence payload | `build_evidence_payload` + demo run |

---

## Setup

### 1. Install Python dependencies

```bash
cd lab06
pip install -r requirements.txt
```

### 2. Configure your MCP server

The `.vscode/mcp.json` has both servers pre-configured with secure prompt-based credentials.

**Track A — Jira:**

The `jira` server uses a locally patched build under `.jira-mcp/` (upgrades the deprecated search API and adds `transition_ticket` and `add_comment` tools).

| Prompt | Value |
|--------|-------|
| Jira site URL | `https://<your-site>.atlassian.net` |
| Jira account email | your Atlassian account email |
| Jira API token | [id.atlassian.com → Security → API tokens](https://id.atlassian.com/manage-profile/security/api-tokens) |

Expected: **jira — Running**, 17 tools discovered.

> **Troubleshooting:** Missing dependencies → `cd .jira-mcp && npm install --production`, then restart.

**Track B — Azure DevOps:**

| Prompt | Value |
|--------|-------|
| ADO org URL | `https://dev.azure.com/<your-org>` |
| Default project | your project name |
| PAT | Personal Access Token with Work Items read/write scope |

Expected: **azure-devops — Running**.

### 3. Seed your backlog (Track B only)

Track A uses the pre-seeded `SCRUM` project in Jira. For Track B, create the epic and stories in your Azure DevOps project:

```text
Using Azure DevOps MCP, create an Epic work item titled:
"Manager Approval Reminder (48h SLA + Audit Trail)"
with description: "FR-001 — Deliver reminder capability for pending approvals older than 48h, with full audit trail."
Return the work item ID as <EPIC-ID>.
```

```text
Using Azure DevOps MCP, create the following Task work items linked as children of <EPIC-ID>:
1. "Define reminder policy and acceptance criteria"
2. "Design reminder data model and audit log schema"
3. "Implement pending-approval age detection service"
4. "Build reminder scheduler for 48-hour threshold"
5. "Build evidence payload for release validation"
Return the IDs as <STORY-1-ID> through <STORY-5-ID>.
```

### 4. Verify connectivity

**Track A:**
```text
Using Jira MCP, list all issues in project SCRUM that are in To Do status.
For each issue return the key, summary, and status.
```
Expected: SCRUM-2 through SCRUM-6 listed as To Do.

**Track B:**
```text
Using Azure DevOps MCP, list all work items in my project that are in To Do state.
Return the ID, title, and state for each.
```
Expected: your 5 stories listed as To Do.

---

## TDD Loop — Follow This for Every Story

For each story:

1. **Read** the function docstring and TODO comment in `reminder_engine.py`.
2. **Run** the pre-written tests — confirm they fail with `NotImplementedError`.
3. **Implement** the function.
4. **Run** the tests again — confirm they pass.
5. **Update your tracker** with a completion comment and story transition.

```bash
# Run all tests at any time
pytest -q tests/test_reminder_engine.py
```

---

## Step 1 — Explore your backlog

**Track A:**
```text
Using Jira MCP, list all issues in project SCRUM that are in To Do status.
For each issue return the key, summary, and status.
```

**Track B:**
```text
Using Azure DevOps MCP, list all work items in my project with state "To Do".
Return ID, title, and state for each.
```

---

## Step 2 — Start the policy story

**Track A:**
```text
Using Jira MCP, move SCRUM-2 to In Progress.
```
```text
Using Jira MCP, assign SCRUM-2 to me.
```

**Track B:**
```text
Using Azure DevOps MCP, update work item <STORY-1-ID> state to "Active" and assign it to me.
```

---

## Step 3 — Post a start comment

**Track A:**
```text
Using Jira MCP, add a comment to SCRUM-2:
"Starting work on reminder policy definition. Will document trigger rules, exclusions, and acceptance criteria."
```

**Track B:**
```text
Using Azure DevOps MCP, add a comment to work item <STORY-1-ID>:
"Starting work on reminder policy definition. Will document trigger rules, exclusions, and acceptance criteria."
```

---

## Step 4 — Read the code and create an implementation plan

Ask Copilot to summarise what to implement:

```text
Show me the file lab06/reminder_engine.py.
Summarise each TODO function, what it should do, and which story it maps to.
```

Then ask Copilot to generate and post a plan for the policy story:

**Track A:**
```text
Create an implementation plan for SCRUM-2 based on the validate_policy function.
Post it as a comment on SCRUM-2 in Jira.
```

**Track B:**
```text
Create an implementation plan for work item <STORY-1-ID> based on the validate_policy function.
Post it as a comment on that work item using Azure DevOps MCP.
```

---

## Step 5 — Implement Story 1: Policy validation

**Function:** `validate_policy(policy: ReminderPolicy) -> None`

- Raise `ValueError` if `threshold_hours <= 0`
- Raise `ValueError` if `cooldown_hours < 0`

**Confirm tests fail first, then implement:**

```bash
pytest -q tests/test_reminder_engine.py -k "Policy or policy"
# Expected: FAILED (NotImplementedError)
```

Implement the function, then confirm:

```bash
pytest -q tests/test_reminder_engine.py -k "Policy or policy"
# Expected: PASSED
```

**Update tracker:**

**Track A:**
```text
Using Jira MCP:
1. Add a comment to SCRUM-2: "validate_policy implemented. Raises ValueError for invalid threshold and negative cooldown. All policy tests passing."
2. Move SCRUM-2 to Done.
3. Move SCRUM-5 to In Progress.
```

**Track B:**
```text
Using Azure DevOps MCP:
1. Add a comment to <STORY-1-ID>: "validate_policy implemented. Raises ValueError for invalid threshold and negative cooldown. All policy tests passing."
2. Update <STORY-1-ID> state to "Closed".
3. Update <STORY-2-ID> state to "Active".
```

---

## Step 6 — Close Story 2: Data model review

The three dataclasses (`Approval`, `ReminderAttempt`, `SchedulerRunResult`) are already fully scaffolded. Review them in `reminder_engine.py`.

**Track A:**
```text
Using Jira MCP:
1. Add a comment to SCRUM-5: "Data model reviewed. Approval, ReminderAttempt, SchedulerRunResult dataclasses are complete."
2. Move SCRUM-5 to Done.
3. Move SCRUM-6 to In Progress.
```

**Track B:**
```text
Using Azure DevOps MCP:
1. Add a comment to <STORY-2-ID>: "Data model reviewed. Approval, ReminderAttempt, SchedulerRunResult dataclasses are complete."
2. Update <STORY-2-ID> state to "Closed".
3. Update <STORY-3-ID> state to "Active".
```

---

## Step 7 — Implement Story 3: Eligibility detection

**Function:** `is_eligible_for_reminder(approval, now, policy) -> tuple[bool, str]`

| # | Condition | Return |
|---|-----------|--------|
| 1 | `status != "PENDING"` | `(False, "approval is not pending")` |
| 2 | Age < `threshold_hours` | `(False, "threshold not reached")` |
| 3 | Reminded within `cooldown_hours` | `(False, "cooldown window still active")` |
| 4 | `manager_email` empty | `(False, "manager email missing")` |
| 5 | All checks passed | `(True, "eligible")` |

```bash
pytest -q tests/test_reminder_engine.py -k eligibility
# Expected after implementation: all PASSED
```

**Track A:**
```text
Using Jira MCP:
1. Add a comment to SCRUM-6: "Eligibility detection implemented. All boundary tests passing."
2. Move SCRUM-6 to Done.
3. Move SCRUM-3 to In Progress.
```

**Track B:**
```text
Using Azure DevOps MCP:
1. Add a comment to <STORY-3-ID>: "Eligibility detection implemented. All boundary tests passing."
2. Update <STORY-3-ID> state to "Closed".
3. Update <STORY-4-ID> state to "Active".
```

---

## Step 8 — Implement Story 4: Scheduler

**`send_reminder(approval, now) -> ReminderAttempt`**
- No `@` in email → `outcome="FAILED"`, `reason="invalid recipient address"`
- Valid email → set `approval.last_reminded_at = now`, `outcome="SENT"`, `reason="reminder dispatched"`

**`run_scheduler_cycle(approvals, policy, now=None) -> SchedulerRunResult`**
1. Resolve `run_at = now or utc_now()`
2. Call `validate_policy(policy)`
3. Find eligible approvals
4. Send reminder for each
5. Return `SchedulerRunResult(evaluated=..., eligible=..., attempts=[...])`

```bash
pytest -q tests/test_reminder_engine.py -k scheduler
# Expected after implementation: all PASSED
```

**Track A:**
```text
Using Jira MCP:
1. Add a comment to SCRUM-3: "Scheduler implemented. send_reminder handles bad email (FAILED) and valid email (SENT). All tests passing."
2. Move SCRUM-3 to Done.
3. Move SCRUM-4 to In Progress.
```

**Track B:**
```text
Using Azure DevOps MCP:
1. Add a comment to <STORY-4-ID>: "Scheduler implemented. send_reminder handles bad email (FAILED) and valid email (SENT). All tests passing."
2. Update <STORY-4-ID> state to "Closed".
3. Update <STORY-5-ID> state to "Active".
```

---

## Step 9 — Implement Story 5: Evidence payload

**Function:** `build_evidence_payload(result: SchedulerRunResult) -> dict`

```python
{
  "evaluated": int,
  "eligible":  int,
  "sent":      int,
  "failed":    int,
  "attempts":  [{"approval_id", "recipient", "attempted_at" (ISO), "outcome", "reason"}]
}
```

```bash
pytest -q tests/test_reminder_engine.py
# Expected: all 8 tests PASSED
```

Generate the evidence artifact:

```bash
python run_sprint_demo.py
# Writes: evidence/fr001_scheduler_run.json
```

**Track A:**
```text
Using Jira MCP:
1. Add a comment to SCRUM-4: "build_evidence_payload implemented and tested. evidence/fr001_scheduler_run.json generated. All 8 tests passing."
2. Move SCRUM-4 to Done.
```

**Track B:**
```text
Using Azure DevOps MCP:
1. Add a comment to <STORY-5-ID>: "build_evidence_payload implemented and tested. evidence/fr001_scheduler_run.json generated. All 8 tests passing."
2. Update <STORY-5-ID> state to "Closed".
```

---

## Step 10 — Release sign-off on the epic

Verify all stories are closed:

**Track A:**
```text
Using Jira MCP, list all issues in project SCRUM and return their key, summary, and status.
```

**Track B:**
```text
Using Azure DevOps MCP, list all work items linked to epic <EPIC-ID> and return their title and state.
```

Write the release recommendation:

**Track A:**
```text
Using Jira MCP, add a comment to SCRUM-1:
"FR-001 sprint increment complete.
Stories closed: SCRUM-2, SCRUM-3, SCRUM-4, SCRUM-5, SCRUM-6.
Evidence: evidence/fr001_scheduler_run.json generated locally.
All unit tests passing (8/8).
Go recommendation: YES — core reminder detection and scheduling implemented and validated with full audit trail."

Then move SCRUM-1 to Done.
```

**Track B:**
```text
Using Azure DevOps MCP, add a comment to work item <EPIC-ID>:
"FR-001 sprint increment complete.
Stories closed: <STORY-1-ID> through <STORY-5-ID>.
Evidence: evidence/fr001_scheduler_run.json generated locally.
All unit tests passing (8/8).
Go recommendation: YES — core reminder detection and scheduling implemented and validated with full audit trail."

Then update <EPIC-ID> state to "Closed".
```

---

## What to Observe

- The **same Copilot prompts** drive both tracks — only the MCP target changes.
- Code is read and edited **locally** — no code-repo MCP is required.
- The **Jira MCP server was patched** (`.jira-mcp/`) to fix a deprecated API and add missing tools, showing that MCP servers are inspectable, extensible software.
- The **Azure DevOps MCP** works out of the box with `@tiberriver256/mcp-server-azure-devops`.
- The evidence JSON is a real release artifact traceable from story to scheduler run.

---

## MCP Tools Reference

### Track A — Jira MCP

| Tool | What it does |
|------|-------------|
| `execute_jql` | Query issues with JQL |
| `transition_ticket` | Move an issue to a new status |
| `add_comment` | Post a comment to an issue |
| `assign_ticket` | Assign an issue to a user |
| `get_ticket` | Fetch a single issue by key |
| `create_ticket` | Create a new issue |
| `query_assignable` | List assignable users in a project |

### Track B — Azure DevOps MCP

| Tool | What it does |
|------|-------------|
| `search_work_items` | Query work items with WIQL |
| `get_work_item` | Fetch a work item by ID |
| `create_work_item` | Create a new work item |
| `update_work_item` | Update fields, state, or assignment |
| `add_work_item_comment` | Post a comment to a work item |
| `list_work_items` | List work items in a project |

---

## Part 2 — Agentic Sprint Execution

**Autonomy Level:** 🔴 Agent acts autonomously — human reviews the final report only

Part 2 replaces the manual prompt-by-prompt workflow from Part 1 with three pre-built
HVE agents that orchestrate the entire sprint lifecycle for you. The same Jira and
Azure DevOps MCP tools are used — but now they are driven by agents, not typed prompts.

> **Complete Part 1 first** (or reset `reminder_engine.py` to all `raise NotImplementedError`
> stubs) before running Part 2 so the agents have real work to do.

---

### The Three Agents

| Agent | File | What it does | Autonomy |
|-------|------|-------------|---------|
| **Story Implementer** | `.github/agents/story-implementer.agent.md` | One story: fetch → implement → test → close | Single story |
| **Sprint Auditor** | `.github/agents/sprint-auditor.agent.md` | Verify all Done → generate evidence → release sign-off | Release gate |
| **Sprint Runner** | `.github/agents/sprint-runner.agent.md` | Full sprint: all five stories + evidence + sign-off | Full sprint |

---

### Using the Story Implementer

Use this when you want to watch a single story get implemented autonomously.

**Track A (Jira):**

```text
@story-implementer story=SCRUM-6 track=jira
```

**Track B (Azure DevOps):**

```text
@story-implementer story=<WORK-ITEM-ID> track=ado
```

The agent will: fetch the story from the tracker → identify the function → read the
docstring contract → implement it → run pytest → post a completion comment → transition
the story to Done.

Repeat for each story, or use the Sprint Runner to do all five at once.

---

### Using the Sprint Auditor

Use this after all stories are Done to generate evidence and post the release sign-off.

**Track A:**

```text
@sprint-auditor epic=SCRUM-1 track=jira
```

**Track A (your stories only):**

```text
@sprint-auditor epic=SCRUM-1 track=jira assignee=me
```

**Track B:**

```text
@sprint-auditor epic=<EPIC-ID> track=ado
```

**Track B (your stories only):**

```text
@sprint-auditor epic=<EPIC-ID> track=ado assignee=me
```

The agent will: list stories (scoped to you if `assignee=me`) → verify all Done →
run 8/8 tests → generate `evidence/fr001_scheduler_run.json` → post the release
sign-off comment → close the epic.

If any story is not Done or any test fails, the agent stops with a blocking report
instead of posting the sign-off.

---

### Using the Sprint Runner (full autonomous sprint)

Use this to complete the entire sprint in one invocation.

**Track A:**

```text
@sprint-runner track=jira
```

**Track A (your stories only):**

```text
@sprint-runner track=jira assignee=me
```

**Track B:**

```text
@sprint-runner track=ado
```

**Track B (your stories only):**

```text
@sprint-runner track=ado assignee=me
```

The agent will execute stories in dependency order (filtered to those assigned to you
if `assignee=me`), run the full test suite after each, generate evidence, and post
the release sign-off — without stopping for human input.

---

### What to Observe in Part 2

Compare Part 2 with Part 1 to understand what agentic autonomy changes:

| Dimension | Part 1 (🟡 Human-approved) | Part 2 (🔴 Autonomous) |
|-----------|---------------------------|------------------------|
| Prompts needed | ~10 per story | 1 per sprint |
| Tracker updates | Human-initiated | Agent-initiated |
| Test feedback loop | Human reads output | Agent reads and self-corrects |
| Release sign-off | Human composes comment | Agent generates from evidence |
| Decision points | Human approves each transition | Agent proceeds on green tests |

Notice that the **code produced is identical** — the agent follows the same docstring
contract you would have used. What changes is how much of the SDLC ceremony is
automated around that implementation.

---

### Agent Constraints (Shared)

All three agents enforce these boundaries to keep the lab safe:

- Never modify `tests/test_reminder_engine.py` or any file in `solutions/`.
- Never force-pass a failing test or skip assertions.
- Never post the release sign-off unless all 8 tests pass.
- Stop and report clearly if any step fails after three attempts.
